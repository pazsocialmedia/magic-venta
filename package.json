{
  "name": "mtg-store",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio estatico para vender una coleccion de Magic. Toma un Google Sheet, lo enriquece con Scryfall + Card Kingdom + cotizacion BCU/BROU y genera un cards.json que consume una web estatica en GitHub Pages.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-data.js",
    "serve": "node -e \"import('node:http').then(({default:h})=>h.createServer((q,s)=>{const f=require('path').join('public',q.url==='/'?'index.html':q.url);require('fs').readFile(f,(e,d)=>{if(e){s.statusCode=404;s.end('404')}else{s.end(d)}})}).listen(8080,()=>console.log('http://localhost:8080')))\""
  },
  "dependencies": {
    "google-auth-library": "^9.14.1"
  },
  "engines": {
    "node": ">=20"
  }
}
